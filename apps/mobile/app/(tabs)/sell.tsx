import { Redirect } from 'expo-router';

export default function SellActionRedirect() {
    // This is a placeholder route for the Tab bar button
    // The actual action is handled by the custom button in _layout.tsx
    return null;
}
